# Yosemite-Lodge-Reservation-Checker
This script automates the hunt for Yosemite National Park lodging. It uses Selenium to bypass stubborn datepickers, handles multi-guest configurations via a config.json file, and runs a background loop to monitor availability. When a room is found, it pings a Discord webhook and launches a visible browser so you can book it instantly.
